# eSports Team Management System - Database Configuration
# For XAMPP (MySQL with root user and no password):
#   db.url=jdbc:mysql://localhost:3306/esports_db?useSSL=false&serverTimezone=UTC
#   db.user=root
#   db.pass=

# Default Configuration (change these for your environment)
db.url=jdbc:mysql://localhost:3306/esports_db?useSSL=false&serverTimezone=UTC
db.user=esports_user
db.pass=Esports@2025

# Application Settings
app.name=eSports Team Management System
app.version=1.0.0
